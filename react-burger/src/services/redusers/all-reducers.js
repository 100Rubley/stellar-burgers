const initialState = {
  ingredients: [],
  currentConstructorIngredients: [],
  currentIngredient: {},
  order: {}
}

export const burgerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
